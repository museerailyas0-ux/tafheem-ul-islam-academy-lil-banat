# No server-side secrets are required.
# The application is compiled to a static website and deployed on Netlify.
# If you configure a custom Google Apps Script URL, it is defined in src/components/AdmissionModal.tsx.
